- name: Loops
  hosts: local
  connection: local
  tasks:
    - name: Print fruits
      ansible.builtin.debug:
        msg: Fruit name - {{ item }}
      loop:
      - apple
      - orange
      - banana
