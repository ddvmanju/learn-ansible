- name: Loops
  hosts: local
  connection: local
  tasks:
    - name: Print furits
      ansible.builtin.debug:
        msg: Fruit name - {{ item }}
      loop:
      - apple
      - orange
      - banana
